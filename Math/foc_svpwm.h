/**
  ******************************************************************************
  * @file    foc_svpwm.h
  * @brief   SVPWM 算法头文件 (基于零序分量注入法)
  * @note    输入: Alpha/Beta 电压; 输出: 驱动 TIM1 的比较值
  ******************************************************************************
  */

#ifndef __FOC_SVPWM_H
#define __FOC_SVPWM_H

#include "main.h" 
#include "tim.h"  // 需要调用 TIM_SetPWM
#include <math.h>

/* ==============================================================================
 * 参数定义
 * ============================================================================== */

// 限制最大占空比，给低侧采样留出窗口
// 0.95 表示上管最多导通 95% 的时间，下管最少导通 5% 的时间
#define MAX_DUTY_CYCLE_LIMIT  0.95f 

// 根号3 的倒数 (1/sqrt(3))
#define _ONE_DIV_SQRT3        0.57735027f
#define _SQRT3_DIV_2          0.86602540f

/* ==============================================================================
 * 函数声明
 * ============================================================================== */

/**
 * @brief  SVPWM 计算核心函数
 * @param  v_alpha: Alpha 轴电压 (伏特)
 * @param  v_beta:  Beta 轴电压 (伏特)
 * @param  v_bus:   当前母线电压 (伏特)
 */
void FOC_SVPWM_Update(float v_alpha, float v_beta, float v_bus);

#endif /* __FOC_SVPWM_H */
